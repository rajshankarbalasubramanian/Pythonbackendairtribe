# Pythonbackendairtribe

Create a virtual environment
python -m venv venv
venv\Scripts\activate

- Install dependencies
pip install django

Run Migrations
python manage.py migrate

Start the server
python manage.py runserver

http://127.0.0.1:8000/api/

  "priority": "critical",
  "reporter_id": 1
}
